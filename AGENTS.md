# Workspace Guidelines

## GitHub Auto-Push Rule
Whenever you make commits to this repository:
1. Ensure all tests pass (`npm test`).
2. Commit your changes with a descriptive message.
3. Automatically push changes to GitHub immediately (`git push origin main`), as requested by the user.
